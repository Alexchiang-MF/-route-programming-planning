# 主線車輛規劃系統 — 下載安裝說明書

> 提供同仁測試使用 / 版本：v1.0 / 日期：2026-04-20

---

## 一、系統需求

| 項目 | 需求 |
|------|------|
| 作業系統 | Windows 10 / 11（64 位元） |
| Python | 3.8 以上版本（建議 3.11） |
| 硬碟空間 | 約 300 MB（含 Python） |
| 記憶體 | 4 GB 以上 |
| 網路 | 首次安裝套件時需連網；執行時不需要 |

---

## 二、下載方式（擇一即可）

### ▶ 方式 A：ZIP 下載（推薦給不熟 Git 的同仁）

1. 打開瀏覽器，前往：
   **https://github.com/Alexchiang-MF/-route-programming-planning**
2. 點擊畫面右上方 **綠色 `< > Code` 按鈕**
3. 選擇 **Download ZIP**
4. 將下載的 `-route-programming-planning-main.zip` **解壓縮**
5. 建議將解壓後的資料夾 **改名為** `主線編成規劃`，放到例如：
   ```
   C:\Users\你的名字\Desktop\主線編成規劃
   ```

### ▶ 方式 B：Git 複製（有裝 Git 的同仁）

開啟命令提示字元，執行：
```bash
git clone https://github.com/Alexchiang-MF/-route-programming-planning.git 主線編成規劃
```

---

## 三、安裝 Python（若尚未安裝）

1. 前往 Python 官網：**https://www.python.org/downloads/**
2. 下載 **Python 3.11（或更新版本）**
3. 執行安裝程式，⚠️ **務必勾選「Add Python to PATH」** 這個選項
4. 點選 **Install Now**，等待安裝完成
5. 驗證安裝：開啟「命令提示字元」(`Win+R` 輸入 `cmd`) 輸入：
   ```
   python --version
   ```
   應顯示類似 `Python 3.11.x` → 成功

❌ 若顯示「不是內部或外部命令」→ Python 未加入 PATH，請重新安裝並勾選該選項。

---

## 四、安裝必要套件

進入剛才解壓的資料夾，**雙擊「安裝套件.bat」**，等待跑完即可。

若雙擊後視窗一閃而過，表示 Python 未裝好，回到第三步驟。

手動安裝方式（備用）：
```bash
cd C:\Users\你的名字\Desktop\主線編成規劃
pip install -r requirements.txt
```

---

## 五、準備 Excel 資料檔 ⚠️ 重要

**本程式從 GitHub 下載後，不包含實際門市資料**（因資料屬於公司內部機密，不上傳公開 repo）。

使用前請向 **Alex / 主管** 取得以下 9 個 Excel 檔案，並放入程式同一資料夾：

```
主線編成規劃\
  ├─ 模組一特殊路線.xlsx
  ├─ 模組二特殊店.xlsx
  ├─ 模組四台北.xlsx
  ├─ 模組四新北.xlsx
  ├─ 模組四基隆.xlsx
  ├─ 模組四宜蘭.xlsx
  ├─ 模組四新竹.xlsx
  ├─ 模組四桃園.xlsx
  └─ 模組四苗栗.xlsx
```

📌 **Excel 欄位格式**（從左到右依序）：
1. 重組類別 / 2. 現況路線 / 3. 現況到店時間 / 4. 店號 / 5. 店名 / 6. 縣市 / 7. 倉別 / 8. 地址 / 9-15. 出貨箱數（7 天）

---

## 六、執行程式

### ▶ 方式一：命令列執行（快速產出 Excel 報表）

**雙擊「執行規劃程式.bat」**

程式會依序顯示：
```
📂 Step 1: 讀取門市資料...
🌍 Step 2: 地理編碼...
🚛 Step 3: 初始化車輛池...
📋 Step 4: 開始路線規劃...
📊 Step 5: 產出 Excel 報表...
🎉 規劃完成！
```

報表自動存放於 `output\路線規劃報告_YYYYMMDD_HHMMSS.xlsx`

### ▶ 方式二：網頁介面（可上傳自訂 Excel、調整參數）

**雙擊「啟動網頁介面.bat」**

瀏覽器會自動開啟 **http://localhost:5001**

功能：
- 逐一上傳各模組 Excel 檔案
- 調整 **路線模式**：迴圈模式（TSP 最短里程）／同心圓模式（70% 行政區 + 30% 環帶）
- 調整 **裝載率**：50% ~ 100%（預設 90%）
- 即時查看規劃進度 log
- 規劃完成後直接下載報表

關閉伺服器：回到 CMD 視窗按 `Ctrl+C`。

---

## 七、輸出報表內容

規劃報表包含以下分頁：
- **路線總覽**：各路線統計、車型、里程、箱數、門市數
- **路線明細**：每條路線的門市清單與到店順序
- **車輛使用**：5 噸/8 噸車使用率、各模組車輛分佈
- **跨縣市整併**：模組四的縣市整併報告（里程節省）
- **警告事項**：超載、時間不足、門市數異常等提醒

---

## 八、常見問題 FAQ

| 問題 | 解決方式 |
|------|----------|
| 雙擊 `.bat` 一閃即消 | Python 未安裝或未加入 PATH，重做第三步 |
| `ModuleNotFoundError: xxx` | 套件未安裝，重做第四步 |
| `找不到 Excel 檔案` | 確認第五步所有檔案都在同一資料夾 |
| 網頁介面開啟後空白 | 等待約 5 秒後重新整理瀏覽器 |
| 港口 5001 被佔用 | 關閉其他佔用程式，或修改 `app.py` 底部的 `port=5001` |
| 跑很久沒反應 | 資料量大時地理編碼需數十秒，請耐心等候 |
| 中文顯示亂碼 | 開啟 CMD 前執行 `chcp 65001` 切換 UTF-8 |

---

## 九、問題回報

測試過程中遇到任何問題，請聯絡：
- **開發負責人**：Alex Chiang
- **GitHub Issues**：https://github.com/Alexchiang-MF/-route-programming-planning/issues

回報時請附上：
1. 錯誤畫面截圖
2. CMD 視窗的錯誤訊息文字
3. 使用的 Excel 檔案名稱
4. 作業系統版本（Win10 / Win11）

---

## 十、更新程式

當 GitHub 上有新版本時：

**方式 A（ZIP 使用者）**：重新下載 ZIP，覆蓋原資料夾即可（記得保留 Excel 資料檔）

**方式 B（Git 使用者）**：在專案資料夾執行
```bash
git pull origin main
```

---

> 🎯 **給測試同仁的建議**：建議先用方式二（網頁介面）試跑一次，比較能直觀看到規劃結果。遇到問題請截圖回報，感謝協助測試！
